# Define the subtotal
subtotal = 0

# Start counting at 10
count = 10


# Loop while count is less than or equal to 50
while count <= 50:

    # Set the current subtotal to the previous subtotal plus the current count
    subtotal = subtotal + count
    print(subtotal)
    count += 1